package enums;

import java.io.Serializable;

/**
 * The TypeCourse enum represents different types of courses.
 * It includes three types: REQUIRED, MINOR, and MAJOR.
 */
public enum TypeCourse implements Serializable {
	REQUIRED, MINOR, MAJOR
}
